# Grid Strongholds Return-Brief Entry

The authenticated Return Brief now exposes `/grid/strongholds` beside Grid Passport in the private player tools area.

The entry intentionally lives on the authenticated return surface rather than the public `/grid` page. This keeps the public launch framing free of runtime-only links while giving returning players a direct path into the NPC stronghold battle loop.

The City Board itself remains untouched because its map integration lane is independently owned.
