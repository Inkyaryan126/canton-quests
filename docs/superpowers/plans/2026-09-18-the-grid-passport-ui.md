# Grid Passport Player UI

Add safe city labels to the authenticated Passport read model, then render a private mobile-first permanent-record screen at `/grid/passport`. The UI uses only CQ-prefixed custom CSS, keeps local seasonal economy out of the screen, handles auth/disabled/no-profile states, and preserves archived city stamps even if a city label is unavailable.
